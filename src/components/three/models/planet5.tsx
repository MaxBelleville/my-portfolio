/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef } from 'react'
import { useGLTF } from '@react-three/drei'
import { useFrame } from '@react-three/fiber'

export function Planet5(props:any) {
  const group = useRef()
  const planetRef = useRef()
  useFrame((state,delta)=>{
    if(planetRef.current)
        planetRef.current.rotation.y+=delta/4;
  })
  const { nodes, materials, animations } = useGLTF('./assets/planet5.gltf')
  return (
    <group ref={group} {...props} dispose={null}>
      <group  scale={1.75} position={[0,-24,-4]} name="Scene">
      <mesh
          ref={planetRef}
          name="planet_frozen"
          castShadow
          receiveShadow
          material={materials.Planet_frozen}
        >
          <sphereGeometry args={[1,16]}/>
        </mesh>
      </group>
    </group>
  )
}

useGLTF.preload('./assets/planet5.gltf')