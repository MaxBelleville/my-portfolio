import type { Metadata } from "next";
import "../../styles/globals.css";
import { getPostInformation, getCaseStudies } from "@/utils/file";

import type { Post } from "@/types";

export default async function BlogList() {
  return <main></main>;
}
